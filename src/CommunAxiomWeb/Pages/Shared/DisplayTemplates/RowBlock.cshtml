                                                                 @model CommunAxiomWeb.Extend.RowBlock

<div class="columns">
    @for (var n = 0; n < Model.Items.Count; n++)
    {    
        @Html.DisplayFor(m => Model.Items[n], Model.Items[n].GetType().Name)
    }
</div>